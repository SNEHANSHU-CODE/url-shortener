/**
 * Models Index
 * Central export for all models
 */

const User = require('./User');
const Url = require('./Url');
const Guest = require('./Guest');

module.exports = {
  User,
  Url,
  Guest,
};
